# Evidencia reproducible de Preview — `4760a50`

Fecha de observación: 2 de agosto de 2026. Alcance: rama
`codex/platform-ux-foundation`, Vercel Preview y base Preview aislada. Production
no fue modificada.

## Artefacto verificado

- commit: `4760a50`;
- deployment: `dpl_8rwZw537MiYbRsPNuvYniTg4NQcP`;
- URL inmutable:
  `https://obrasaas-saas-8lyjmms4q-marcelos-projects-c26aa499.vercel.app`;
- alias de rama:
  `https://obrasaas-saas-git-codex-platf-eb1140-marcelos-projects-c26aa499.vercel.app`;
- estado observado: `Ready`.

## Validación local previa

- `npm.cmd test`: `2003/2003` pruebas completas;
- suite focalizada del ledger: `24/24` pruebas;
- `npm.cmd run lint`: sin errores;
- `npm.cmd run build`: build optimizado completado, con 88 páginas y las rutas
  nuevas de inventario incluidas;
- `npm.cmd exec prisma validate`: esquema válido;
- `npm.cmd audit --omit=dev --audit-level=high`: `0` vulnerabilidades;
- `git diff --check`: sin errores.

## Migración y verificador PostgreSQL

El release aprobó el gate de identidad de Preview, detectó 115 migraciones y
aplicó exactamente:

- `20260802170000_inventory_stock_ledger`.

El verificador dedicado terminó correctamente con este resultado:

```text
Verified 20260802170000_inventory_stock_ledger: exact accepted putaway, one-shot reversal, immutable ledger, non-negative DB-owned balances and no inferred backfill.
```

El build remoto continuó después del gate y el deployment llegó a `Ready`. Este
corte acredita que la migración, sus invariantes y el artefacto compilado
corresponden al mismo commit.

## Smokes sin sesión del artefacto exacto

| Superficie | Ruta | Resultado |
| --- | --- | --- |
| URL inmutable | `/` | `200` |
| URL inmutable | `/dashboard/purchases` | `404`, esperado para superficie privada sin sesión |
| URL inmutable | `/api/inventory-items?active=true` | `404`, esperado para API privada sin sesión |
| URL inmutable | `/api/inventory-transactions?sourceInspectionId=smoke` | `404`, esperado para API privada sin sesión |
| Alias de rama | `/` | `200` |
| Alias de rama | `/dashboard/purchases` | `404`, esperado para superficie privada sin sesión |
| Alias de rama | `/api/inventory-items?active=true` | `404`, esperado para API privada sin sesión |

No se observó ninguna respuesta `5xx` en estos smokes.

## Límite de la evidencia

Este corte acredita validación local, migración/verificador PostgreSQL, build y
borde HTTP **no autenticado** de Vercel Preview. No acredita todavía:

- recorrido UI autenticado por rol ni comportamiento de las APIs con una sesión
  real;
- BOM, reservas, consumo, devolución, transferencia, ajustes ni el estado
  derivado `AVAILABLE`;
- entrega Resend observada o E2E de Meta WhatsApp;
- Production.

Production permaneció intacta. `onHand` significa existencia física registrada;
no equivale por sí solo a disponibilidad para una tarea.
