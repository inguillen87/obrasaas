# Evidencia reproducible de Preview — `3181807`

Fecha de observación: 2 de agosto de 2026, 06:49 ART (`UTC-03:00`). Alcance: rama `codex/platform-ux-foundation`, Vercel Preview y Neon Preview aislado. Production no fue modificada.

## Artefacto verificado

- commit: `3181807`;
- deployment: `dpl_GdrLvspbHK7ttZEA7EW88WGzX4Me`;
- URL inmutable: `https://obrasaas-saas-hg6t5w33u-marcelos-projects-c26aa499.vercel.app`;
- alias de rama: `https://obrasaas-saas-git-codex-platf-eb1140-marcelos-projects-c26aa499.vercel.app`;
- estado observado: `Ready`.

## Migración y verificadores

El release aprobó el gate de identidad de Preview y el preflight read-only del outbox, que encontró `0` filas incompatibles antes de migrar. Prisma detectó 113 migraciones y aplicó:

- `20260802150000_goods_receipt_commitment_allocation`;
- `20260802151000_goods_receipt_commitment_allocation_guard_fix`.

El verificador dedicado confirmó en PostgreSQL real:

- checksums exactos de ambas migraciones;
- FKs compuestas por organización, obra, OC, partida, remito y compromiso;
- cantidades `Decimal(14,3)` y rechazo exacto de un exceso de `0.001`;
- aislamiento cross-tenant con error controlado antes de la FK;
- ledger append-only y guards `ENABLE ALWAYS`;
- remito `VOIDED` terminal y liberación de cobertura sin reactivación silenciosa;
- serialización mediante advisory lock observada con dos conexiones.

Los verificadores históricos también pasaron y el build de Next.js 16.2.11 terminó antes de desplegar los outputs.

## Fallos seguros observados

Dos builds anteriores quedaron en estado `Error` y no sustituyeron el Preview estable:

1. `dpl_DvGkGtGjuuqdjVTTRXcd15msDxtX` detectó que el verificador de compromisos asumía un catálogo cerrado de triggers y no toleraba el trigger aditivo de conciliación. Se corrigió para verificar sus nombres gobernados sin bloquear migraciones posteriores.
2. El build siguiente detectó que `FOUND` no se actualiza por `EXECUTE` dinámico en PL/pgSQL. Las FKs seguían rechazando el cruce de tenant, pero no con el error gobernado esperado. Se agregó la migración correctiva `151000`; no se reescribió una migración ya aplicada.

Ambos casos prueban el comportamiento fail-closed del pipeline; no constituyen evidencia de disponibilidad.

## Smokes del artefacto exacto

| Ruta sin credenciales | Resultado |
| --- | --- |
| `/` | `200` |
| `/dashboard/purchases` | `404`, superficie privada opaca detrás de Clerk |
| `/api/goods-receipt-commitment-allocations?purchaseOrderId=smoke` | `404`, API privada opaca detrás de Clerk |
| `/api/cron/notifications` | `401`, bearer obligatorio |

La consulta de logs del deployment por respuestas `5xx` no devolvió eventos después de estos smokes.

## Límite de la evidencia

Este corte acredita migración, contrato PostgreSQL, build y borde HTTP no autenticado de **Preview**. No acredita todavía recorrido UI autenticado por rol, aceptación/daño/rechazo, stock o reserva, envío Resend, Production ni E2E de Meta WhatsApp.
