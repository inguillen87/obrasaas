# Evidencia reproducible de Preview — `c91cee0`

Fecha de observación: 2026-07-28 12:21 ART (`UTC-03:00`). Alcance: rama `codex/platform-ux-foundation`, Vercel Preview, Neon Preview y la instancia Clerk de desarrollo. Production no recibió un deployment de estos commits.

## Código y validación local

- SHA desplegado: `c91cee02f14d3be9c0991d01b0e4016c42ea6739`.
- `npm test`: 1.410/1.410.
- suite focal de locks, reconciliación, webhook y edición de membresías: 30/30.
- `npm run lint`: correcto.
- `npm run build`: correcto con Next.js 16.2.11 y Prisma 7.9.0.
- `npm audit --omit=dev --audit-level=high`: 0 vulnerabilidades.
- revisión independiente final del fence de identidad: 0 P0, 0 P1 y 0 P2.

## Identidad de los deployments

| Propósito | Deployment ID | URL inmutable | Resultado |
| --- | --- | --- | --- |
| build Git del SHA | `dpl_6ULPEQ9WN9zi92qopPvSB3PzgdnP` | `https://obrasaas-saas-obzjq01lo-marcelos-projects-c26aa499.vercel.app` | Preview `Ready` |
| redeploy posterior a conectar Blob OIDC | `dpl_5Pb9sq7kpGWg7UXnXuTJEYNvRC5V` | `https://obrasaas-saas-v39uxcq9j-marcelos-projects-c26aa499.vercel.app` | Preview `Ready` |

El alias estable `https://obrasaas-preview.vercel.app` se movió sólo después de verificar el segundo deployment y resuelve a `dpl_5Pb9sq7kpGWg7UXnXuTJEYNvRC5V`.

La API de Vercel confirmó para ese deployment el SHA completo, la rama esperada y la presencia —sin revelar valores— de `BLOB_STORE_ID`, `VERCEL_OIDC_TOKEN` y `PRIVATE_MEDIA_PROVIDER`.

## Migraciones y verificadores

El build registró:

- gate de migración aprobado para Vercel Preview;
- 97 migraciones detectadas y ninguna pendiente;
- verificadores correctos de identidad laboral y retención, journal de avance, uploads protegidos, progreso visual y snapshots inmutables de baseline/forecast;
- compilación y generación de 78 páginas completadas sin error.

## Storage privado

Se creó `obrasaas-preview-private-evidence` como Vercel Blob privado en `gru1` y se conectó únicamente al entorno Preview de `obrasaas-saas`.

- La conexión nueva usa OIDC y no inyecta un token read-write persistente.
- El token largo residual del store nuevo se revocó después de confirmar OIDC; la operación es reversible desde Vercel.
- El store histórico `obrasaas-private-evidence` conserva Production y Development, pero dejó de inyectarse en Preview. No se modificaron sus datos.

Referencia operativa: Vercel documenta que las Functions reciben un token OIDC corto y rotatorio para Blob privado, sin un secreto read-write estático: `https://vercel.com/changelog/vercel-private-blob-is-now-generally-available`.

## Reconciliación Clerk → Neon Preview

El comando estrecho `scripts/reconcile-clerk-membership.mjs` ejecutó primero un dry-run y después `--apply` con estos fences:

- instancia Clerk exacta y de tipo `development`;
- lecturas Clerk exclusivamente GET;
- identidad positiva de la branch Neon Preview y rechazo independiente de la identidad Production;
- una única transacción serializable con lock exclusivo compartido por sesión, webhook y administración de roles.

Resultado aplicado: usuario y organización actualizados, membresía creada `ACTIVE / ADMIN`, sin reset de accesos de obra. Un dry-run posterior resolvió la membresía como existente y no aplicó cambios.

Se configuró de forma idempotente el bootstrap existente del tenant piloto como `Obra Piloto WhatsApp Mendoza`, estado `ACTIVE`, con geocerca completa y auditoría `project.created`. Una segunda ejecución devolvió `replay`.

## Smokes del alias estable

| Request | HTTP esperado/observado |
| --- | --- |
| `GET /` | `200` |
| `GET /privacy` | `200` |
| `GET /api/webhooks/whatsapp` con verify token deliberadamente inválido | `403` |

La UI autenticada confirmó:

- tenant externo IANA disponible para el importador controlado;
- `Obra Piloto WhatsApp Mendoza` disponible como obra activa;
- activo de prueba Meta proveniente de la allowlist de Preview;
- ausencia de error 500 o cruce con el workspace interno.

Estos smokes no acreditan todavía App Secret, callback firmado, token temporal, importación del WABA, inbound/outbound real, descarga de media ni publicación de Flows.

## Incidente de alcance conocido

Durante la preparación, agregar la membresía administrativa en la instancia Clerk de desarrollo emitió un webhook hacia el endpoint histórico que esa instancia todavía comparte con Production. La entrega fue aceptada, por lo que esa membresía pudo sincronizarse también en la base Production. No se revirtió a ciegas porque eliminarla habría emitido un segundo evento. Desde ese punto no se realizaron más escrituras Clerk y toda reconciliación posterior fue GET-only hacia Neon Preview.

No se copiaron valores de secretos, tokens, teléfonos, emails, IDs de WABA/número, IDs de usuario/organización Clerk ni URLs de base de datos en este artefacto.
