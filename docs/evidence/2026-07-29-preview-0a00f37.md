# Evidencia reproducible de Preview — `0a00f37`

Fecha de observación: 2026-07-29 00:02 ART (`UTC-03:00`). Alcance: rama `codex/platform-ux-foundation`, Vercel Preview, Neon Preview y smoke no personal de OpenAI. No hubo deployment ni migración de estos commits en Production; se conserva por separado el incidente histórico de posible sincronización de membresía Clerk documentado en [2026-07-28-preview-c91cee0.md](./2026-07-28-preview-c91cee0.md).

## Código y calidad local

- commit funcional: `8c3e7e3` (`feat(ai): make visual dispatch cost auditable`);
- hotfix PostgreSQL: `0a00f37` (`fix(db): defer AI ledger retention check`);
- suite completa: 1.538/1.538;
- Prisma válido/generado, ESLint y build Next.js 16.2.11 correctos;
- auditoría de dependencias de producción: 0 vulnerabilidades;
- PostgreSQL 17.5 efímero desde cero: 100 migraciones, verifier completo y cascada tenant `0/0/0/0`;
- no se encontraron claves, tokens ni teléfonos en el diff.

## Hallazgos del gate remoto

El gate remoto falló de forma segura dos veces antes de aceptar un candidato:

1. una deployment CLI sin metadata Git intentó usar la identidad genérica de Preview; el build la reconoció como identidad Production y abortó antes de migrar;
2. con la rama correcta, Neon aplicó el esquema y PostgreSQL real detectó que el FK inmediato `AiDispatchBudgetReservation_daily_ledger_fkey` hacía depender el borrado del tenant del orden no garantizado de cascadas hermanas.

No se reescribió la migración `080000`, ya registrada por Neon. La migración incremental `090000` conserva `ON DELETE NO ACTION` y vuelve ese FK `DEFERRABLE INITIALLY DEFERRED`: el borrado directo del ledger sigue fallando con `23503`, mientras el borrado atómico del tenant elimina reserva, ledger, recibo y obra sin residuos.

## Deployment aceptada

| Campo | Evidencia |
| --- | --- |
| Deployment nativa de Git | `dpl_4UZQuEuj8ddkL4wi6zUVcxABB7WU` |
| URL inmutable | `https://obrasaas-saas-2lh6u0tmg-marcelos-projects-c26aa499.vercel.app` |
| Rama / commit | `codex/platform-ux-foundation` / `0a00f37` |
| Estado | Preview `READY` |
| Alias estable | `https://obrasaas-preview.vercel.app` |

Se seleccionó la deployment nativa de Git porque recibió correctamente las variables `Sensitive` específicas de la rama. Una deployment CLI paralela llegó a `READY`, pero no recibió esas variables en runtime y no fue aliasada. Esto coincide con la [guía de Vercel sobre metadata Git y variables branch-specific](https://vercel.com/kb/guide/branch-variables-and-domains-not-linked-to-cli-deployments); no se deshabilitó Deployment Protection.

El log remoto de la candidata confirmó:

- gate de identidad Preview aprobado;
- 100 migraciones detectadas, sin pendientes;
- lifecycle durable de media WhatsApp verificado;
- lifecycle, despacho, presupuesto concurrente y recibos de IA verificados con smoke rollback-only;
- compilación Next.js completa.

## Smokes sobre URL inmutable y alias estable

| Request | HTTP observado | Interpretación |
| --- | ---: | --- |
| `GET /` | 200 | aplicación pública disponible |
| `GET /privacy` | 200 | política pública disponible |
| `POST /api/webhooks/whatsapp` sin firma | 401 | App Secret presente y firma fail-closed |
| `GET /api/cron/webhooks` sin secreto | 401 | cron fail-closed |
| `POST /api/superadmin/ai-cost-reconciliations` sin sesión | 404 | superficie interna opaca detrás de Clerk |

Después de esos smokes, `obrasaas-preview.vercel.app` se reasignó a la URL inmutable anterior y repitió los mismos resultados.

## Presupuesto y smoke OpenAI

- `AI_VISUAL_DAILY_BUDGET_MICROS=1000000` quedó configurado sólo para Preview de esta rama: USD 1 por tenant/día UTC; no es secreto ni modifica Production.
- La clave OpenAI dedicada permanece en el gestor de secretos de Vercel y no se copió a Git, comandos guardados ni evidencia.
- Se ejecutó el adapter real contra `gpt-5.6-sol` con la imagen pública de marca de ObraSaaS, sin personas, documentos ni evidencia de obra. El smoke directo usó una semilla de seguridad efímera para no extraer el secreto `Sensitive` del runtime.
- Resultado: abstención `not_construction_progress`; no inventó porcentaje.
- Telemetría normalizada completa: 1.330 tokens de entrada, 203 de salida, 0 cached y 0 cache-write; costo calculable aproximado USD 0,01274 con la [tarifa oficial observada](https://developers.openai.com/api/docs/pricing).
- No se conservaron IDs de proveedor, prompt, respuesta cruda ni bytes privados.

Este smoke valida autenticación, modelo, Structured Output, visión, política de cache y telemetría del adapter. No atravesó una evaluación tenant en la aplicación ni el ledger remoto, y por lo tanto no reemplaza el journey Meta → storage → evidencia → evaluación → revisión → costo.

## Gates externos todavía abiertos

- app Meta sin publicar y negocio sin verificar; publicación/revisión aplicable fuera del control del código;
- credencial del WABA/tenant aún no importada por Integraciones y sin inbound/estados reales;
- sin foto real de obra, comentario, GPS correlacionado ni benchmark consentido;
- DPA/retención/ZDR o Modified Abuse Monitoring de OpenAI aún deben resolverse antes de datos reales;
- sin smoke UI autenticado de replay/conciliación ni escenario forecast generado después de revisión humana;
- Production no recibió estos commits, migraciones, presupuesto ni nueva credencial.

Por estos límites, el corte certifica **infraestructura Preview gobernada**, no piloto WhatsApp E2E ni función productiva.
