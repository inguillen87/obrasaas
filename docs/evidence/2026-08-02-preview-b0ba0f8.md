# Evidencia reproducible de Preview — `b0ba0f8`

Fecha de observación: 2 de agosto de 2026, 09:14 ART (`UTC-03:00`). Alcance: rama `codex/platform-ux-foundation`, Vercel Preview y Neon Preview aislado. Production no fue modificada.

## Artefacto verificado

- commit: `b0ba0f8`;
- deployment: `dpl_ATmLy3oypBeNvSi7dXbMRM5zpQaY`;
- URL inmutable: `https://obrasaas-saas-6723gq2v0-marcelos-projects-c26aa499.vercel.app`;
- alias de rama: `https://obrasaas-saas-git-codex-platf-eb1140-marcelos-projects-c26aa499.vercel.app`;
- estado observado: `Ready`.

## Validación local previa

- `1975/1975` pruebas completas;
- `57/57` pruebas focalizadas durante la revisión integrada;
- ESLint y `git diff --check` sin errores;
- `npm audit --omit=dev --audit-level=high`: `0` vulnerabilidades;
- Prisma Client 7.9.0 generado y build optimizado de Next.js 16.2.11 completado.

La reproducción PGlite posterior al último fence transaccional no llegó a ejecutarse porque el runtime auxiliar quedó trabado. No se contabiliza como evidencia. El gate autoritativo que sigue es el verificador dedicado sobre Neon Preview, documentado abajo.

## Migración y verificador PostgreSQL

El release aprobó el gate de identidad de Preview y el preflight read-only del outbox, que encontró `0` filas incompatibles. Prisma detectó 114 migraciones y aplicó:

- `20260802160000_goods_receipt_inspection_exceptions`.

El verificador dedicado confirmó en Neon Preview:

- partición exacta y diferida de cada línea inspeccionada entre `ACCEPTED`, `DAMAGED`, `REJECTED` y `QUARANTINED`;
- finalizaciones, correcciones y reversiones append-only e inmutables;
- cierre final de faltantes derivado sólo de asignaciones e inspecciones vigentes, sin backfill inferido;
- snapshot histórico de código/nombre de ubicación y rechazo de ubicaciones inactivas para nuevas inspecciones;
- aislamiento compuesto por organización, obra, OC, remito, línea, compromiso y ubicación;
- cantidades decimales exactas, rechazo de `NaN` y guards `ENABLE ALWAYS`;
- serialización por advisory lock y rechazo del orden transaccional `cierre → disposición` hasta revertir el cierre activo;
- congelamiento de remito, líneas y asignaciones cuando existe evidencia de inspección vigente.

Los verificadores históricos también pasaron y el build optimizado terminó antes de desplegar los outputs.

## Smokes del artefacto exacto

| Ruta sin credenciales | Resultado |
| --- | --- |
| `/` | `200` |
| `/dashboard/purchases` | `404`, superficie privada opaca detrás de Clerk |
| `/api/goods-receipts?limit=1` | `404`, API privada opaca detrás de Clerk |
| `/api/goods-receipt-inspections?...` | `404`, API privada opaca detrás de Clerk |
| `/api/inventory-locations` | `404`, API privada opaca detrás de Clerk |
| `/api/supplier-commitment-line-closures?...` | `404`, API privada opaca detrás de Clerk |

Las respuestas privadas incluyeron `X-Clerk-Auth-Reason: protect-rewrite` y `X-Clerk-Auth-Status: signed-out`; no se observó ninguna respuesta `5xx` en estos smokes.

## Límite de la evidencia

Este corte acredita migración, invariantes PostgreSQL, build y borde HTTP no autenticado de **Preview**. No acredita todavía recorrido UI autenticado por rol, evidencia fotográfica tipada/firma jurídica de recepción, ledger de stock, reservas o BOM, envío Resend, Production ni E2E de Meta WhatsApp. Una cantidad `ACCEPTED` no equivale todavía a stock `AVAILABLE`.
