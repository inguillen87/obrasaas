import { createHash } from 'node:crypto';
import { AccessError, accessErrorResponse, getPlatformAccess, requireTenantPermission } from '@/lib/access';
import { uploadProtectedFile } from '@/lib/cloudinary';

const MAX_BYTES = 10 * 1024 * 1024;
const ALLOWED = new Set(['image/jpeg', 'image/png', 'image/webp', 'application/pdf']);
function errorResponse(error) { if (error instanceof AccessError) return accessErrorResponse(error); return Response.json({ error: error.message || 'No se pudo cargar el comprobante.', code: 'CASH_RECEIPT_UPLOAD_FAILED' }, { status: error.status || 500 }); }
export async function POST(request) { try { const access = await getPlatformAccess(); requireTenantPermission(access, 'org:execution:manage', { subscriptionMode: 'write' }); const form = await request.formData(); const file = form.get('file'); if (!(file instanceof File) || file.size < 1 || file.size > MAX_BYTES) return Response.json({ error: 'El comprobante debe pesar entre 1 byte y 10 MB.', code: 'CASH_RECEIPT_SIZE_INVALID' }, { status: 400 }); if (!ALLOWED.has(file.type)) return Response.json({ error: 'Tipo de comprobante no permitido.', code: 'CASH_RECEIPT_TYPE_INVALID' }, { status: 400 }); const digest = createHash('sha256').update(Buffer.from(await file.arrayBuffer())).digest('hex'); const upload = await uploadProtectedFile(file, { folder: `obrasaas/projects/${access.project.id}/cash-receipts`, context: `project=${access.project.id}|sha256=${digest}|private=true`, idempotencyKey: `cash-receipt:${access.project.id}:${digest}`, resourceType: file.type === 'application/pdf' ? 'raw' : 'image' }); return Response.json({ receipt: { provider: 'cloudinary', storage: { provider: 'cloudinary', assetId: upload.assetId || null, publicId: upload.publicId || null, resourceType: upload.resourceType || null, format: upload.format || null, bytes: upload.bytes || file.size }, mimeType: file.type, filename: file.name, size: file.size, sha256: digest, visibility: 'private' } }, { status: 201, headers: { 'Cache-Control': 'private, no-store' } }); } catch (error) { return errorResponse(error); } }
