'use client';
import { useState } from 'react';
import styles from '../extra-work/extra-work.module.css';
async function api(path, options = {}) { const response = await fetch(path, { ...options, headers: { 'Content-Type': 'application/json' } }); const body = await response.json().catch(() => ({})); if (!response.ok) throw new Error(body.error || 'No se pudo completar la operación.'); return body; }
export default function PayablesClient({ initialInvoices, suppliers, projectName }) { const [rows, setRows] = useState(initialInvoices); const [notice, setNotice] = useState(null); const [busy, setBusy] = useState(false); const [form, setForm] = useState({ supplierId: suppliers[0]?.id || '', invoiceNumber: '', amount: '', currency: 'ARS', dueAt: '', operationKey: '' }); async function create(event) { event.preventDefault(); setBusy(true); try { const result = await api('/api/supplier-invoices', { method: 'POST', body: JSON.stringify({ ...form, amount: Number(form.amount), operationKey: crypto.randomUUID(), dueAt: form.dueAt || undefined }) }); setRows((current) => [result.invoice, ...current]); setNotice('Factura registrada.'); } catch (error) { setNotice(error.message); } finally { setBusy(false); } } async function decide(row, status) { setBusy(true); try { const result = await api('/api/supplier-invoices', { method: 'PATCH', body: JSON.stringify({ id: row.id, expectedRevision: row.revision, status }) }); setRows((current) => current.map((entry) => entry.id === row.id ? { ...entry, status: result.status, revision: result.revision } : entry)); setNotice(`Factura: ${result.status}.`); } catch (error) { setNotice(error.message); } finally { setBusy(false); } } return <main className={styles.shell}><header><span>S10 · cuentas por pagar</span><h1>Facturas de proveedores</h1><p>{projectName} · vencimientos y decisiones auditables.</p></header>{notice && <p className={styles.notice}>{notice}</p>}<section className={styles.panel}><h2>Nueva factura</h2>{suppliers.length === 0 ? <p>Configurá proveedores antes de registrar facturas.</p> : <form onSubmit={create}><select value={form.supplierId} onChange={(event) => setForm({ ...form, supplierId: event.target.value })}>{suppliers.map((supplier) => <option key={supplier.id} value={supplier.id}>{supplier.legalName}</option>)}</select><input required value={form.invoiceNumber} onChange={(event) => setForm({ ...form, invoiceNumber: event.target.value })} placeholder="Número de factura" /><input required type="number" min="0.01" step="0.01" value={form.amount} onChange={(event) => setForm({ ...form, amount: event.target.value })} placeholder="Importe" /><input type="date" value={form.dueAt} onChange={(event) => setForm({ ...form, dueAt: event.target.value })} /><button disabled={busy}>Registrar factura</button></form>}</section><section className={styles.panel}><h2>Obligaciones</h2><ul>{rows.length === 0 ? <li>No hay facturas registradas.</li> : rows.map((row) => <li key={row.id}><div><strong>{row.invoiceNumber} · {row.supplier?.legalName}</strong><span>{row.status} · {row.currency} {row.amount} · vencimiento {row.dueAt ? row.dueAt.slice(0, 10) : 'sin fecha'}</span></div>{row.status === 'RECEIVED' && <button disabled={busy} onClick={() => decide(row, 'APPROVED')}>Aprobar</button>}{row.status === 'APPROVED' && <button disabled={busy} onClick={() => decide(row, 'PAID')}>Marcar pagada</button>}{!['PAID', 'VOIDED'].includes(row.status) && <button disabled={busy} onClick={() => decide(row, 'VOIDED')}>Anular</button>}</li>)}</ul></section></main>; }
