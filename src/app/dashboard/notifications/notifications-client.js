'use client';
import { useState } from 'react';
import styles from './notifications.module.css';
export default function NotificationsClient({ initialNotifications, projectName }) { const [items, setItems] = useState(initialNotifications); const [notice, setNotice] = useState(null); async function markRead(item) { const response = await fetch('/api/notifications', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ id: item.id }) }); if (!response.ok) { setNotice('No se pudo marcar la notificación.'); return; } setItems((current) => current.map((entry) => entry.id === item.id ? { ...entry, status: 'READ', readAt: new Date().toISOString() } : entry)); } return <main className={styles.shell}><header><span>Operación trazable</span><h1>Notificaciones</h1><p>{projectName} · alertas generadas por eventos de la obra.</p></header>{notice && <p className={styles.notice}>{notice}</p>}<section className={styles.panel}>{items.length === 0 ? <p>No hay notificaciones.</p> : <ul>{items.map((item) => <li className={item.status === 'READ' ? styles.read : ''} key={item.id}><div><strong>{item.title}</strong><span>{item.createdAt} · {item.status}</span><p>{item.body}</p></div>{item.status !== 'READ' && <button type="button" onClick={() => markRead(item)}>Marcar leída</button>}</li>)}</ul>}</section></main>; }
